
import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import PrimaryButton from '../PrimaryButton';

<Meta title='Commons|PrimaryButton/doc'/>

## Primary Button

**Primary Button** allow users to take actions, and make choices, with a single tap.
It is also use to distinguish to their users.

## Props

<Props of={PrimaryButton} />